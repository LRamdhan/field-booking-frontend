import { css } from "@emotion/react"

const Footer = () => {

  return (
    <nav css={css`background-color: var(--blur-color); padding: 15px 0; height: max-content; text-align: center;`}>Footer</nav>
  )
}

export default Footer